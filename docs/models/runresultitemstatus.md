# RunResultItemStatus

## Example Usage

```python
from clay.models import RunResultItemStatus

# Open enum: unrecognized values are captured as UnrecognizedStr
value: RunResultItemStatus = "complete"
```


## Values

This is an open enum. Unrecognized values will not fail type checks.

- `"complete"`
- `"failed"`
